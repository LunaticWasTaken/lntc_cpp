/**
 * Here goes any test-code for lntc_cpp.
 */
#include "../lntc_cpp.hpp"
#include <iostream>

#define log(msg) std::cout << msg << std::endl
#define logf(fmt, ...) std::cout << std::format(fmt, __VA_ARGS__) << std::endl

// Runtime Entrypoint
int main(int argc, char* argv[]) {
  // ...
  return 0;
}